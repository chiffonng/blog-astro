# https://pagefind.app/docs/config-options/
# globally search "data-pagefind" to search for inline config
# WARNING: Must build the site before running "npx pagefind"
site: 'dist'
serve: true
verbose: true
